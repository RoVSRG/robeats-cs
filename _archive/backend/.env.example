# Backend Environment Example
# Copy to .env or .env.local and adjust values.

# Database (adjust for local Docker Postgres)
DATABASE_URL="postgresql://robeats:password@localhost:5432/robeats?schema=public"

# Redis / Valkey
VALKEY_HOST=localhost
VALKEY_PORT=6379

# API Security
API_KEY=dev_change_me

# Node
NODE_ENV=development
PORT=3000
