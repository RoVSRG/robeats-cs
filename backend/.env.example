# You can use these exact values in your real .env file for local development with Docker. Just copy this file to .env and you're good to go.

# Postgres (local Docker)
DATABASE_URL=postgresql://robeats:robeats_password_change_in_production@127.0.0.1:5432/robeats?schema=public

# Valkey/Redis (local Docker)
VALKEY_HOST=127.0.0.1
VALKEY_PORT=6379
VALKEY_TLS=false
VALKEY_DB=0
VALKEY_USERNAME=
VALKEY_PASSWORD=

# API
API_KEY=dev_change_me
NODE_ENV=development
