{
  "name": "sdk",
  "sourceRoot": "packages/sdk/src",
  "projectType": "library",
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run generate",
        "cwd": "packages/sdk"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["generate"],
      "options": {
        "command": "echo 'SDK generated successfully'",
        "cwd": "packages/sdk"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm install",
        "cwd": "packages/sdk"
      }
    }
  },
  "tags": ["scope:sdk", "type:library"]
}