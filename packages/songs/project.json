{
  "name": "songs",
  "sourceRoot": "packages/songs",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "lune run build.luau",
        "cwd": "packages/songs"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "packages/songs"
      }
    }
  },
  "tags": ["scope:songs", "type:library"]
}