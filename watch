#!/bin/bash
# Simple wrapper script for cross-platform file watching
if command -v lune >/dev/null 2>&1; then
    lune run scripts/watch.luau
else
    echo "Error: Lune is not installed or not in PATH"
    echo "Please run: aftman install"
    exit 1
fi
